{{!-- Standalone class-detail window. The ClassView (shared with the character
     creator) is rendered imperatively into the content region by the app, so the
     template is just a scroll shell. The window root carries
     `dauligor-character-creator`, so the ClassView markup gets the creator's
     identical styling. --}}
<div class="dauligor-class-detail">
  <div class="dauligor-class-detail__body" data-region="content"></div>
</div>
